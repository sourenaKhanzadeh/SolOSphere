pragma solidity ^0.6.0;
contract Opt_unOptimizedContract63 {
    uint public balance = 0;
	constructor() public {
        	}
	function add(uint amount) public {
        balance += amount;
	}
	function subtract(uint amount) public {
        balance -= amount;
	}
}
