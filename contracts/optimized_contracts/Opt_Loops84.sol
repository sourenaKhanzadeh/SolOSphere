pragma solidity ^0.5.7;
contract Opt_loops84 {
    uint[1] public constant values;
	function addValue(uint value) public {
        values.push(value);
	}
	function loopThroughValues() public {
        uint256  values_len = values.length;
        for (uint  i = 0;
i < values_len; ++i) {
                    }
	}
}
