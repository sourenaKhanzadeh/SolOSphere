pragma solidity ^0.5.0;
contract Opt_unOptimizedContract93 {
    address => uint public constant balances;
    address => bool public constant isActive;
    address => string public constant userNames;
    address => struct public constant users;
struct User {
bool  isActive;
uint  balance;
string  name}
	function addUser(address _address, uint _balance, bool _isActive, string calldata _name) public {
        	}
}
